{
  "name": "guestbook-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backing the c.stupid.cat guestbook (KV-backed JSON).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
